name: animated_weight_picker
description: The Animated Weight Picker Flutter package is a customizable, animated weight picker widget for use in a Flutter application. It allows users to select a weight value by swiping or dragging a handle along a scale. The appearance and behavior of the widget can be configured through various properties and callbacks.
version: 1.0.2
repository: https://github.com/guru-technolabs/animated_weight_picker
# This package supports all platforms listed below.
platforms:
  android:
  ios:

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
license:
   - file: LICENSE

