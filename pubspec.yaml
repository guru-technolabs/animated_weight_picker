name: animated_weight_picker
description: Animated Weight Picker allows to select a weight by swiping or dragging a handle along a scale.
version: 1.0.3
repository: https://github.com/guru-technolabs/animated_weight_picker
# This package supports all platforms listed below.
platforms:
  android:
  ios:

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
license:
   - file: LICENSE

